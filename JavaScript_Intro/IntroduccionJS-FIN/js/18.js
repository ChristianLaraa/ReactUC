//CONDICIONALES

const disponible = 200
const retirar = 200

const auth = true

if(disponible >= retirar) {
    //se cumple la condicion
    console.log('Si puedes retirar')
} else {
    //no se cumple la condicion
    console.log('Saldo insufucuente')
}

/**
 *  > mayor que
 *  < menor que
 * >= mayor o igual
 * <= menor o igual
 *  == igual
 *  === igual estricto
 *  !== diferente a 
 */ 